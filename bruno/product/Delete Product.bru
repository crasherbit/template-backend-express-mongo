meta {
  name: Delete Product
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/product/:id
  body: none
  auth: none
}

params:path {
  id: {{productId}}
}

assert {
  res.status: eq 200
  res.body.message: eq OK
}
