info:
  name: List Passkeys
  type: http
  seq: 8

http:
  method: GET
  url: "{{BASE_URL}}/auth/passkeys"
  auth: none

assert:
  - name: res.status
    value: eq 200

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
