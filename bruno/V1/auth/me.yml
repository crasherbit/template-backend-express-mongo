info:
  name: Me
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/auth/me"
  auth: none

assert:
  - name: res.status
    value: eq 200
  - name: res.body.payload.username
    value: isDefined

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
