info:
  name: Login Begin
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/auth/login/begin"
  body:
    type: json
    data: |-
      {
          "username": "crasherbit"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
