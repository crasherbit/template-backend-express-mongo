info:
  name: Logout
  type: http
  seq: 7

http:
  method: POST
  url: "{{BASE_URL}}/auth/logout"
  auth: none

assert:
  - name: res.status
    value: eq 204

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
