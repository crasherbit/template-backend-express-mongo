info:
  name: Health Check
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/health"
  body:
    type: none
  auth: none

assert:
  - name: res.status
    value: eq 200
  - name: res.body.status
    value: eq OK

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
